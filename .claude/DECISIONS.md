# Undecided / Needs Decision

> 1 decision remains. 52/52 plan items delivered (100%).

| # | Category | Question | Context |
|---|----------|----------|---------|
| 1 | Deploy Target | Docker Compose → fly.io, railway, AWS? | `docker compose up` + `npx expo start --web` both verified. All tests pass. Ready to deploy. |

Resolved:
- ~~Refresh Tokens~~ → DB-backed with rotation implemented
- ~~Storage Backend~~ → MinIO SDK integrated, local default
- ~~Video Download~~ → expo-file-system + expo-sharing, download button on video screen
