# Undecided / Needs Decision

> 1 decision remains. The project is otherwise complete.

| # | Category | Question | Context |
|---|----------|----------|---------|
| 1 | Deploy Target | Docker Compose → fly.io, railway, AWS? | `docker compose up` + `npx expo start --web` both verified. All tests pass. Knowledge graph built. Ready to deploy. |

Resolved:
- ~~Refresh Tokens~~ → DB-backed with rotation implemented
- ~~Storage Backend~~ → MinIO SDK integrated, local default
